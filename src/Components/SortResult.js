import React from "react";
import styled from "styled-components";

const sortResult = ({ sortResult }) => {
  return <Wrapper>{sortResult}</Wrapper>;
};
const Wrapper = styled.div``;
export default sortResult;
